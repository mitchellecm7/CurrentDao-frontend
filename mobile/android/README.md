Android project files live here for native build and permission config.

- Update `AndroidManifest.xml` for camera/gps/notification permissions.
- Ensure minSdkVersion 26 in `build.gradle`.
- Use `./gradlew assembleRelease` for store builds.
