iOS project files live here for native build and entitlement config.

- Set deployment target to iOS 14.0 in Xcode.
- Add Camera/GPS/Notifications privacy strings in Info.plist.
- Run `pod install` in `ios/` before building.
